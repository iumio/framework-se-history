#########################################
### IUMIO .htaccess for web directory ###
### IUMIO VERSION 0.0.1 ------------- ###
### CREATED BY RAFINA DANY ---------- ###
### <danyrafina@gmail.com> ---------- ###
#########################################

DirectoryIndex iumio_dev.php

<IfModule mod_negotiation.c>
    Options -MultiViews
</IfModule>

<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /web/

    RewriteCond %{REQUEST_URI}::$1 ^(/.+)/(.*)::\2$
    RewriteRule ^(.*) - [E=BASE:%1]

    # Sets the HTTP_AUTHORIZATION header removed by apache
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]


    RewriteCond %{ENV:REDIRECT_STATUS} ^$
    RewriteRule ^iumio_dev\.php(/(.*)|$) %{ENV:BASE}/$2 [R=301,L]


    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule .? - [L]

    # Rewrite all other queries to the front controller.
    RewriteRule .? %{ENV:BASE}/iumio_dev.php [L]
</IfModule>

<IfModule !mod_rewrite.c>
    <IfModule mod_alias.c>

        RedirectMatch 302 ^/$ /iumio_dev.php/

    </IfModule>
</IfModule>
