route:
  name : default_start_{appname}
  path : /index
  activity : Default%showIndex
endroute

route:
  name : default_test_parameter_{appname}
  path : /index/{hi}
  activity : Default%index
endroute